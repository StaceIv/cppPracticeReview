#include <iostream>

using namespace std;

//define enum types
enum days {Monday, Tuesday, Wednesday,
     		Thursday, Friday, Saturday, Sunday};

enum direction {North=1, South=-1, East=1, West=-1};

int main(int argc, char *argv[]){

     days dow; //declare a variable of type days

     dow = Friday;

     cout << "value of dow is: " << dow << endl;

     direction dir1(East), dir2(South);
     cout << "Value of dirs = " << dir1 << " " << dir2 << endl;

     return 0;
}
